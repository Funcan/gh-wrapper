package main

// resolveUser determines the target GitHub user for the given working directory.
// Returns "" if no user is configured (no switch needed).
func resolveUser(cwd string) (string, error) {
	return "", nil
}
