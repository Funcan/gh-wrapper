package main

// runner.go: exec gh, handle signals, restore user

// run executes gh with the given args, switching to targetUser first if set.
// Returns the exit code from gh.
func run(args []string, targetUser string) int {
	return 0
}
