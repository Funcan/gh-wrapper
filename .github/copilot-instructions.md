# Copilot Instructions

After making any code changes, always run the following commands in order:

1. `make fmt` — format all Go source files
2. `make build` — ensure the project compiles
3. `make test` — run tests and verify coverage
